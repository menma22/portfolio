---
// src/components/DetailModal.astro
// グラスモーフィズムの詳細レポートモーダルコンポーネント
---

<div id="detail-modal" class="fixed inset-0 z-50 hidden items-center justify-center p-4">
    <!-- オーバーレイ背景 -->
    <div id="modal-overlay" class="absolute inset-0 bg-black/60 backdrop-blur-sm"></div>
    
    <!-- モーダルコンテンツ -->
    <div id="modal-content" class="relative w-full max-w-4xl max-h-[90vh] overflow-y-auto rounded-3xl 
        bg-white
        border border-gray-200 
        shadow-2xl
        transform transition-all duration-500 scale-95 opacity-0">
        
        <!-- ヘッダー部分 -->
        <div class="sticky top-0 z-10 flex items-center justify-between p-6 border-b border-gray-200 bg-white/95 backdrop-blur-xl">
            <h2 id="modal-title" class="text-2xl md:text-3xl font-bold text-gray-900">
                プロダクト詳細
            </h2>
            <button id="modal-close" class="p-2 rounded-full bg-gray-100 hover:bg-gray-200 border border-gray-200 transition-colors group">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6 text-gray-600 group-hover:text-gray-900 transition-colors">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
        
        <!-- コンテンツ本体 -->
        <div class="p-8">
            <!-- レポート本文 -->
            <div id="modal-report" class="markdown-body">
                <!-- レポート内容がここに挿入される -->
            </div>
            
            <!-- アクションボタン -->
            <div class="flex flex-col sm:flex-row gap-4 pt-8 mt-8 border-t border-gray-200">
                <a id="modal-link" href="#" target="_blank" rel="noopener noreferrer" 
                    class="flex-1 flex items-center justify-center gap-3 px-8 py-4 rounded-2xl 
                    bg-gradient-to-r from-blue-600 to-purple-600 
                    hover:from-blue-500 hover:to-purple-500
                    text-white font-semibold text-lg
                    shadow-lg shadow-blue-500/25 hover:shadow-blue-500/40
                    transform hover:scale-[1.02] transition-all duration-300">
                    <span>サイトを開く</span>
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                    </svg>
                </a>
                <button id="modal-close-btn" 
                    class="flex-1 flex items-center justify-center gap-3 px-8 py-4 rounded-2xl 
                    bg-gray-100 hover:bg-gray-200
                    border border-gray-200 hover:border-gray-300
                    text-gray-700 font-semibold text-lg
                    transform hover:scale-[1.02] transition-all duration-300">
                    閉じる
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    #detail-modal.active {
        display: flex;
    }
    
    #detail-modal.active #modal-content {
        transform: scale(1);
        opacity: 1;
    }
    
    /* スクロールバーのスタイリング */
    #modal-content::-webkit-scrollbar {
        width: 8px;
    }
    
    #modal-content::-webkit-scrollbar-track {
        background: #f1f5f9;
    }
    
    #modal-content::-webkit-scrollbar-thumb {
        background: #cbd5e1;
        border-radius: 4px;
    }
    
    #modal-content::-webkit-scrollbar-thumb:hover {
        background: #94a3b8;
    }
    
    /* Markdown スタイリング */
    .markdown-body {
        color: #374151;
        font-size: 1rem;
        line-height: 1.8;
    }
    
    .markdown-body h1 {
        display: none;
    }
    
    .markdown-body h2 {
        font-size: 1.5rem;
        font-weight: 700;
        margin-top: 2.5rem;
        margin-bottom: 1rem;
        padding-bottom: 0.75rem;
        color: #1e40af;
        border-bottom: 3px solid #3b82f6;
    }
    
    .markdown-body h2:first-of-type {
        margin-top: 0;
    }
    
    .markdown-body h3 {
        font-size: 1.125rem;
        font-weight: 600;
        margin-top: 1.5rem;
        margin-bottom: 0.75rem;
        padding-left: 1rem;
        color: #1f2937;
        border-left: 4px solid #8b5cf6;
    }
    
    .markdown-body p {
        margin-bottom: 1rem;
    }
    
    .markdown-body strong {
        color: #111827;
        font-weight: 600;
    }
    
    .markdown-body ul {
        list-style: none;
        padding-left: 0;
        margin-bottom: 1.5rem;
    }
    
    .markdown-body ul li {
        margin-bottom: 1rem;
        padding-left: 1.5rem;
        position: relative;
    }
    
    .markdown-body ul li::before {
        content: "▸";
        position: absolute;
        left: 0;
        color: #8b5cf6;
        font-weight: bold;
    }
    
    .markdown-body blockquote {
        background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
        border-left: 4px solid #f59e0b;
        border-radius: 0.5rem;
        padding: 1.25rem 1.5rem;
        margin: 1.5rem 0;
        font-style: normal;
    }
    
    .markdown-body blockquote p {
        margin-bottom: 0.5rem;
        color: #92400e;
    }
    
    .markdown-body blockquote p:last-child {
        margin-bottom: 0;
    }
    
    .markdown-body blockquote strong {
        color: #78350f;
    }
    
    .markdown-body hr {
        border: none;
        height: 1px;
        background: #e5e7eb;
        margin: 2rem 0;
    }
</style>

<script>
    import { marked } from 'marked';
    
    // markedの設定
    marked.setOptions({
        breaks: true,
        gfm: true
    });
    
    // モーダル制御スクリプト
    const modal = document.getElementById('detail-modal');
    const modalOverlay = document.getElementById('modal-overlay');
    const modalClose = document.getElementById('modal-close');
    const modalCloseBtn = document.getElementById('modal-close-btn');
    
    // ====================================================
    // 🎨 プロジェクト詳細データ（Markdown形式）
    // ====================================================
    const projectDetails: Record<string, {
        title: string;
        projectUrl: string;
        markdown: string;
    }> = {
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        // 📱 New Fashion - AI Look Book
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        'New Fashion - AI Look Book': {
            title: 'New Fashion - AI Look Book',
            projectUrl: 'https://ai-vr-fashion-try-on-7ssg.bolt.host/coordinate',
            markdown: `
# New Fashion - AIバーチャル試着アプリ

## 1. プロダクト概要
* **コンセプト**
  「買う前に、着る。ファッションECの新しいスタンダード」
* **概要**
  生成AI（Gemini API）を活用し、自分の写真に気になった服を自然に合成できるバーチャル試着アプリケーション。試着から購入、コーデ共有までをワンストップで提供します。

## 2. 開発のきっかけ・背景
**「ネット通販の『思ってたのと違う』をなくしたい」**

私自身ファッションが大好きですが、ECサイトでの購入には常に「サイズ感や雰囲気が自分に合うか分からない」というリスクが伴います。「買ってみたけど着なかった」という失敗や、返品の手間を減らしたいと強く思っていました。

そんな中、**Gemini Nano / Pro**（生成AI）の画像生成能力に触れ、「これを応用すれば、実際に服を着なくても、まるで着ているかのような自然な試着画像が作れるのではないか？」と確信し、開発に着手しました。

## 3. 主な機能と体験
* **AIバーチャル試着（Virtual Try-On）**
  ユーザーの全身写真と、ECサイト上の服の画像をAIに読み込ませることで、瞬時に着用画像を生成。「自分の手持ちの服に、新しい服が合うか」を視覚的に確認できます。
* **バーチャル・クローゼット**
  気になった服や生成したコーデ画像をSupabase上に保存。購入前の検討リストとしてだけでなく、手持ち服の管理ツールとしても機能します。
* **コーデシェアSNS（構想・一部実装）**
  作成したバーチャルコーデをInstagramのようにシェアできる機能。「服を買うお金がなくても、センスだけで勝負できる」世界観を目指し、インフルエンサーがバーチャル上でスタイリングを提案できるプラットフォームを構想しました。

## 4. 技術的な課題と考察
**「見た目」と「物理的なサイズ感」の壁**

開発を通して直面した最大の課題は、**「生成AIは『着ている風』の画像を作るのは得意だが、厳密な『サイズ感（着丈・袖丈）』の再現は苦手である」**という点です。

AIはピクセル単位の合成は行いますが、物理シミュレーションを行っているわけではないため、「SサイズとMサイズの違い」などを正確に描写することに限界を感じました。
これは現在のファッションテック業界全体の課題でもありますが、今後は3Dモデリング技術との併用などで解決策を探っていきたいと考えています。

## 5. 現在のステータスについて（重要）
> **※ポートフォリオ閲覧時の注意点**
> 
> 本アプリはSupabaseの無料枠（プロジェクト数上限）の関係で、現在はデータベース接続を停止した**「アーカイブモード（UI確認用）」**として公開しています。
> データの保存や新規の試着生成は行えませんが、フロントエンドのデザインや画面遷移の挙動はご確認いただけます。
            `
        },
        
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        // 🏕️ Vagabonds Camp
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        'Vagabonds Camp': {
            title: 'Vagabonds Camp',
            projectUrl: 'https://vagabonds-camp.vercel.app/',
            markdown: `
# Vagabonds Camp - 学生のための無料AI議事録

## 1. プロダクト概要
* **コンセプト**
  「月額1000円の壁を壊す。学生のための、完全無料・高性能AI議事録」
* **概要**
  Gemini APIの無料枠を活用し、既存の有料ツール同等の議事録作成・タスク管理を「実質0円」で提供するWeb完結型のSaaS。
* **ターゲット**
  予算はないが活動を効率化したい大学生チーム、研究室（ゼミ）、サークル。

## 2. 開発のきっかけ・背景
**「便利な技術は、資金力のある企業だけでなく、学生こそが自由に使えるべきだ」**

既存のAI議事録ツールは非常に便利ですが、月額1000円〜という価格設定は、収入の少ない学生にとって高いハードルです。「技術的には低コストで実現可能なはずなのに、なぜ学生が使えないのか？」という疑問から開発をスタートしました。

また、私自身が会議中にメモを取ることに必死で、肝心の「議論」や「会話」に集中できていないという課題もありました。会話に含まれる個人の思考やアイデンティティをデジタル資産として残すためにも、誰もが手軽に使える議事録ツールが必要だと考えました。

## 3. 解決策（ビジネスモデル）
### ユーザー自身のAPIキーを使う「BYOKモデル」
サービス側でAPIコストを負担するのではなく、ユーザー自身がGeminiのAPIキー（無料枠）を入力して利用する仕組みを採用しました。これにより、開発者の運用コストをゼロにしつつ、ユーザーは最新の高性能AI（Gemini 3.0 preview）を無料である程度制限なく使えるようにし、学生にとって理想的なSaaSを実現しました。

## 4. 技術的な工夫・実装詳細
単に「文字起こしをする」だけでなく、システムとして使いやすくするために以下の工夫を凝らしました。

### JSON出力による機能連携（プロンプトエンジニアリング）
AIへの指示（プロンプト）を調整し、出力を単なる文章ではなく、厳密な「JSON形式」で返させるように設計しました。これにより、議事録の本文とは別に「To-Doリスト」や「決定事項」をデータとしてきれいに抽出することに成功。アプリ上のタスク管理機能へ、自動的にTo-Doが登録される仕組みを作りました。

### マークダウンと目次の自動生成
長時間の会議でも振り返りやすいよう、出力形式をマークダウンに指定。さらに、見出しタグを解析して「目次」を自動生成する機能を実装しました。ユーザーは目次をクリックするだけで、聞きたいトピックへ瞬時に飛ぶことができます。

### データ消失を防ぐ「保存優先」のアーキテクチャ
開発当初、AIの生成エラー時に録音データごと消えてしまう問題がありました。そこで処理フローを見直し、**「まず音声データをSupabaseに保存する」**ことを最優先にしました。その上でAI処理を走らせることで、もし生成に失敗しても、保存されたデータから何度でも「再試行」ができるようになり、大事な会議データが絶対に消えない信頼性を担保しました。

## 5. ユーザーからのフィードバックと改善
実際に大学の友人や知人にこのアプリを使ってもらいました。「AIってこんなに便利なんだ！」と、AIの可能性を肌で感じてもらえた時は本当に嬉しかったです。

しかし、運用していく中で**「録音した音声データを後からダウンロードできないのが不便だ」**という厳しい意見も貰いました。そこで、すぐにバックエンドの処理を見直し、音声ファイルのダウンロード機能を実装して再度使ってもらいました。

その結果、「これで安心して使える」と言ってもらえ、**「アプリは作って終わりではなく、ユーザーの声を聞いて改善し続けることで、初めて本当の価値が生まれる」**ということを深く学びました。

## 6. 今後の展望
### 会話データの資産化（ライフログ）
会議だけでなく、日常会話も常時録音・蓄積できる機能を実装予定です。蓄積された膨大なデータを将来的にAIに学習させ、自分の思考や知識を完全に外部化した「自分専用AIエージェント」を作ることを目指しています。

### Voice to Action
スマホに話しかけるだけで、カレンダー登録やToDo消化が完了する音声インターフェースの拡充を進めていきます。
            `
        },

        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        // 🌍 UNESCO-VIEW
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        'UNESCO-VIEW': {
            title: 'UNESCO Globe',
            projectUrl: 'https://menma22.github.io/unesco-globe/',
            markdown: `
# UNESCO Globe - インタラクティブ3D地球儀

## 1. プロダクト概要
* **コンセプト**
  「世界遺産の壮大さを、掌の上で回す」
* **概要**
  Webブラウザ上で動作するインタラクティブな3D地球儀。世界遺産の座標がマッピングされており、タップするとその場所へ地球が回転し、詳細が表示されるビジュアル・アプリケーション。
* **目的**
  最新AIモデル（Gemini 3）のコーディング能力、特に3Dグラフィックス領域におけるベンチマーク検証。

## 2. 開発の動機・背景
**「最新AIは、高度な3D描画を理解できるのか？」**

私は新しいAIモデルがリリースされるたびに、その限界を知るための「ベンチマーク開発」を行っています。以前のモデルでは、Three.jsを用いた「3Dの球体生成」や「衛星写真のテクスチャマッピング（球体への貼り付け）」といった空間認識を伴うコード生成は精度が低く、実装には多くの修正が必要でした。

「最新の『Gemini 3』ならば、この壁を越えられるのではないか？」という技術的な問いから、複雑な3D処理を要するこのアプリの開発に着手しました。

## 3. 技術的な検証とプロセス
### 「One-Shot」精度の向上と対話による修正
検証の結果、Gemini 3は初期プロンプトだけで「球体の生成」から「座標計算」までの骨格をほぼ一発で出力することに成功しました。一方で、生成された地球儀が真っ暗になる（テクスチャが反映されない）などのバグも発生しましたが、AIと対話しながら「光源の設定」や「マテリアルのプロパティ」を調整することで解決。

このプロセスを通じ、AIを単なるコード生成機としてだけでなく、**「3D開発のペアプログラミング相手」**として活用するノウハウを蓄積しました。

## 4. こだわりの機能・UX
**世界遺産にふさわしい「壮大さ」の表現**

単なる地図アプリではなく、世界遺産の持つ「歴史」や「スケール感」を感じられるアートワークとして設計しました。

### 衛星写真マッピングと3Dアニメーション
Three.jsを使用し、リアルな衛星写真を球体にマッピング。遺産を選択すると、地球儀が滑らかに回転してその地点を画面中央に捉えるカメラワークを実装し、没入感を高めました。

### 没入感を高めるUIデザイン
情報はシンプルに表示し、あくまで「地球」が主役となるデザインを意識。宇宙から地球を眺めているような、知的で洗練されたビジュアル体験を目指しました。

## 5. プロジェクトを通しての学び
### 3Dライブラリ（Three.js）の理解
AIにコードを書かせるだけでなく、生成されたコード（メッシュ、ジオメトリ、マテリアル、カメラ座標系など）を読み解くことで、3Dグラフィックスの基礎原理を深く理解しました。

### AIとの共創
最新モデルの「得意なこと（論理構築）」と「苦手なこと（微細なレンダリング設定）」を肌感覚で理解し、エンジニアがどこを補うべきかという勘所を養いました。
            `
        },

        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        // 📰 BRIEFLY
        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        'BRIEFLY': {
            title: 'BRIEFLY',
            projectUrl: 'https://favorite-news-latter.vercel.app/',
            markdown: `
# BRIEFLY - AI情報収集プラットフォーム

## 1. プロダクト概要
* **コンセプト**
  「検索する時間はもう古い。世界中の情報を、AIがあなたにデリバリー」
* **概要**
  YouTube、Webニュース、プレスリリースなど、分散する情報をAIが自動で収集・選別・要約する「自分専用のインテリジェンス・プラットフォーム」。
* **現在のステータス**
  第一弾として、最も時間コストのかかる「YouTube動画」の自動要約機能を実装・公開中。

## 2. 開発のきっかけ・背景
**「情報の洪水（Information Overload）をハックする」**

現代は情報に溢れており、有益な情報を逃さずキャッチアップすることは困難です。特にAIなどの技術トレンドは、毎日大量のYouTube動画や記事で発信されますが、それらを全て視聴・検索して回るのは物理的に不可能です。また、「AI活用のハッカソン」や「注目企業のプレスリリース」など、少し目を離した隙に重要な機会を逃してしまう経験も多々ありました。

「人間が情報を探しに行くのではなく、情報の方から人間に届くべきだ」と考え、能動的な「検索」をなくし、待っているだけで必要な情報が集まるツールを目指して開発を始めました。

## 3. 現在の解決策・主な機能
まずは情報の密度が高く、最も消化に時間がかかる「動画コンテンツ」の攻略から着手しました。

### YouTube情報の自動レポート化
登録したチャンネルの更新を検知し、Gemini APIを用いて「要約」と「詳細レポート」を即座に生成します。「見る」のではなく「読む」ことで、情報収集時間を1/10に圧縮します。

### 要約と詳細の2段階表示
* **要約**
  「見る価値があるか？」を30秒で判断するためのサマリー。
* **詳細レポート**
  動画内の重要な発言や数値を細部までトレースした長文レポート。

### モダンなUI設計
大量の情報をストレスなく処理できるよう、アコーディオン（折りたたみ）UIを採用。必要な情報だけを展開して読めるようにし、情報の視認性を高めました。

## 4. 技術的な挑戦・工夫
### 二重生成を防ぐ排他制御ロジック
自動更新システムにおいて、同じ動画のレポートが重複して生成される問題が発生しました。APIコストの無駄を防ぐため、生成前に「Video ID」と「動画タイトル」の二重チェックを行うロジックを実装。無駄なAPIコールを完全に防ぎ、運用の安定性を高めました。

### YouTube API × Gemini APIの連携
字幕データの取得からAIへのプロンプト投入、翻訳（多言語対応）までをパイプライン化。特に字幕がない動画への対応や、長時間の動画をコンテキストウィンドウに収める工夫を行いました。

### BYOK（Bring Your Own Key）モデル
ユーザー自身のGemini APIキーを利用する設計にし、学生開発ながら持続可能な運用モデルを構築しました。

## 5. 今後の展望・構想
**「検索」の完全自動化プラットフォームへ**

YouTubeだけでなく、Web上のあらゆる情報を統合する「All-in-One 情報収集エージェント」への進化を開発中です。

### AI検索エージェント（Autonomous Search Agent）
ユーザーが気になっているキーワード（例：「○○のライブ情報」「株式会社△△のプレスリリース」）を登録しておくだけで、AIエージェントが毎日Web全体を巡回・検索します。

### 「待ち」の情報収集体験
単に検索結果を羅列するのではなく、前回との差分や重要度をAIが判断。「新しいライブが決まりました！」といった価値ある情報だけを選別して通知します。

これにより、ユーザーは検索する手間をゼロにし、**「待っているだけで、世界で一番早く欲しい情報が届く」**体験を実現します。
            `
        }
    };
    
    function openModal(projectTitle: string) {
        const project = projectDetails[projectTitle];
        if (!project) return;
        
        // モーダルにデータを設定
        const modalTitle = document.getElementById('modal-title');
        const modalReport = document.getElementById('modal-report');
        const modalLink = document.getElementById('modal-link') as HTMLAnchorElement;
        
        if (modalTitle) modalTitle.textContent = project.title;
        if (modalReport) {
            // MarkdownをHTMLに変換
            modalReport.innerHTML = marked.parse(project.markdown) as string;
        }
        if (modalLink) modalLink.href = project.projectUrl;
        
        // モーダルを表示
        modal?.classList.add('active');
        document.body.style.overflow = 'hidden';
    }
    
    function closeModal() {
        modal?.classList.remove('active');
        document.body.style.overflow = '';
    }
    
    // イベントリスナー
    modalOverlay?.addEventListener('click', closeModal);
    modalClose?.addEventListener('click', closeModal);
    modalCloseBtn?.addEventListener('click', closeModal);
    
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeModal();
    });
    
    // グローバルに公開
    (window as any).openDetailModal = openModal;
</script>
